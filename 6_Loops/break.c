#include<stdio.h>
int main()
{
    int n;
    while(n != 0)
   {
        if(n = 0)
        break;    //if n = 0 then break statement break the loop
        printf("enter the number:");
       scanf("%d",&n);    
    }
    printf("whenever 0 enter the loop is ended.");
}