#include<stdio.h>
    int main()
    {
        int num, i;
        printf("enter the num");
        scanf("%d",&num);
        //while(num>=0)
        do
        {
            printf(" %d",num);
            num--;
        } while(num>=0);
    }
